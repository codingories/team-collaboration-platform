language: java
jdk: openjdk8

cache:
  directories:
    - ~/.m2  # Cache Maven dependencies to speed up builds

script: mvn clean verify

after_failure:
  - cat target/surefire-reports/*.txt  # Print test failure details in logs